# Quick Deploy — CommercePulse_AI_Network

Use Render → New Web Service → Docker.

```text
Name: commercepulse-ai-network
Root Directory: .
Dockerfile Path: Dockerfile
Disk Mount: /var/data
DATA_DIR=/var/data
NODE_ENV=production
```

For full instructions, see `RENDER_DEPLOYMENT_GUIDE.md`.
